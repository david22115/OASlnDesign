# ==============================================================================
# TaskMaster Monorepo Environment Configuration
# ==============================================================================
# This file contains the local environment settings for development.
# Copy this file to .env and modify as needed. DO NOT commit .env with secrets.

# ------------------------------------------------------------------------------
# 1. Database (PostgreSQL)
# ------------------------------------------------------------------------------
# Prisma 資料庫連線字串 (Schema: postgresql://USER:PASSWORD@HOST:PORT/DB)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/oa_system"

# ------------------------------------------------------------------------------
# 2. Redis & Infrastructure
# ------------------------------------------------------------------------------
# Redis 連線位址，用於 JWT 黑名單與任務序列 (BullMQ)
REDIS_URL="redis://localhost:6379"

# ------------------------------------------------------------------------------
# 3. API Server (Back-end)
# ------------------------------------------------------------------------------
# API 伺服器運行的埠號
PORT=3001

# CORS 跨域授權來源位址 (通常指向前端 Portal 位址)
CORS_ORIGIN="http://localhost:3000"

# ------------------------------------------------------------------------------
# 4. Security & JWT
# ------------------------------------------------------------------------------
# JWT 簽發金鑰 (生產環境務必使用複雜長字串且禁止外洩)
JWT_SECRET="super_secret_key_change_me_in_prod"

# JWT 有效期限 (支援格式: 1h, 7d, 30m)
JWT_EXPIRES_IN="1h"

# ------------------------------------------------------------------------------
# 5. Frontend (Client-side)
# ------------------------------------------------------------------------------
# 前端 Portal 或 Admin 呼叫後端 API 的基礎位址
# (Next.js 規定必須以 NEXT_PUBLIC_ 開頭才可在瀏覽器端存取)
NEXT_PUBLIC_API_URL="http://localhost:3001"
